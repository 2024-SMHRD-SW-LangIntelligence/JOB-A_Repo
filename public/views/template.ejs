<!DOCTYPE html>
<html lang="en">

<head>
    <link href='https://fonts.googleapis.com/css?family=Arvo' rel='stylesheet' type='text/css'>
    <link href='https://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
    <!-- <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet"> -->

    <link rel="shortcut icon" href="../img/메인아이콘-removebg-preview.png" sizes="50x50" type="image/png">

    <!-- 직접만든 css들 -->
    <link rel="stylesheet" href="../css/total.css">
    <link rel="stylesheet" href="../css/topbar.css">
    <link rel="stylesheet" href="../css/studyGruop.css">

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/limonte-sweetalert2/7.2.0/sweetalert2.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/limonte-sweetalert2/7.2.0/sweetalert2.all.min.js"></script>
    <script src='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.14/index.global.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/uuid/8.3.2/uuid.min.js'></script>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>
        <%= title %>
    </title>
    <!-- Custom fonts for this template-->
    <link href="vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
    <link
        href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i"
        rel="stylesheet">
    <!-- Custom styles for this template-->
    <link href="css/sb-admin-2.min.css" rel="stylesheet">
    <style>
        @import url('https://cdn.jsdelivr.net/gh/moonspam/NanumSquare@2.0/nanumsquare.css');

        /* 날짜 글씨 색상을 검은색으로 설정 */
        .fc-daygrid-day {
            position: relative;
        }

        .fc-daygrid-day-events {
            max-height: 45px;
            /* Adjust this height as needed */
        }

        .fc-event-title,
        .fc-daygrid-event-dot,
        .fc-daygrid-day-number,
        .fc-list-event-title,
        .fc-col-header-cell,
        .fc-col-header-cell-cushion,
        .fc-day-header,
        .fc-toolbar-title {
            text-decoration: none !important;
        }

        .fc-daygrid-day-number {
            color: black;
        }

        .fc-col-header-cell-cushion {
            color: black;
        }

        .fc-day-sat .fc-daygrid-day-number {
            color: blue;
        }

        .fc-day-sun .fc-daygrid-day-number {
            color: red;
        }

        .colorbox {
            display: flex;
            flex-wrap: wrap;
            width: 165px;
            margin-top: 10px;
            border: 2px solid #cccccc;
            border-radius: 10px;
            background-color: #f9f9f9;
        }

        .color-radio {
            display: none;
        }

        .color-label {
            display: inline-block;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            margin: 10px;
            cursor: pointer;
            border: 2px solid transparent;
        }

        .color-label:hover {
            border-color: #333;
        }

        .color-radio:checked+.color-label {
            border-color: #000;
        }
    </style>
    <style>
        .swiper_text {
            margin-bottom: 200px;
        }
    </style>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <style>
        @font-face {
            font-family: 'CustomFont';
            /* 사용할 폰트 패밀리 이름 설정 */
            src: url('../img/CookieRun\ Bold.ttf') format('truetype');
            /* 폰트 파일 경로 지정 */
            /* 추가적으로 웹폰트 형식에 따라 필요한 경우 다른 포맷도 지정 가능 */
        }

        .caption {
            display: inline !important;
            position: absolute;
            font-family: Arial, sans-serif;
            font-size: 12px !important;
            padding: 10px;
            padding-left: 17px;
            width: 220px;
            height: 90px;
            margin-left: 450px;
            transform: translateY(-50%);
            animation: move 18s linear infinite;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 24px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            background-color: #333;
        }

        .swiper {
            width: 1920px;
            height: 916px;
            background-color: aliceblue;
        }

        .swiper-slide img {
            width: 80%;
            height: 75%;
            margin-top: 20px;
        }

        .swiper-wrapper {
            margin-top: 15px;
            text-align: center;
        }

        .slide-description {
            font-weight: bold;
            color: #0f3f8b;

        }

        .swiper_h1 {
            font-family: 'CustomFont', serif;
            font-weight: bold;
            text-align: center;
            margin-top: 10px;
            color: #0c3a85;
            font-size: 50px;
        }
    </style>
</head>

<body id="page-top">
    <!-- Page Wrapper -->


    <div id="wrapper">
        <%- include('sidebar') %>
            <!-- Content Wrapper -->
            <div id="content-wrapper" class="d-flex flex-column">
                <!-- Main Content -->
                <div id="content">
                    <%- include('topbar') %>
                        <!-- Begin Page Content -->
                        <div class="container-fluid">
                            <div class="custom-row">
                                <% if(user){%>
                                    <!-- Calendar Column -->
                                    <div class="calendar-column">
                                        <!-- Calendar -->
                                        <div id='calendar' style="height: 1100px; width: 1100px; margin-left: 50px;">
                                        </div>

                                        <!-- Modal add-->
                                        <div class="modal fade" id="eventModal" tabindex="-1"
                                            aria-labelledby="eventModalLabel" aria-hidden="true">
                                            <div class="modal-dialog">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <h5 class="modal-title" id="eventModalLabel">일정 입력</h5>
                                                        <button type="button" class="btn-close" data-bs-dismiss="modal"
                                                            aria-label="Close"></button>
                                                    </div>
                                                    <form id="eventForm">
                                                        <div class="modal-body">
                                                            <div class="mb-3">
                                                                <label for="eventTitle" class="form-label">일정 제목</label>
                                                                <input type="text" class="form-control" id="eventTitle"
                                                                    name="msche_title" required
                                                                    style="margin-bottom: 10px;">
                                                                날짜<br>
                                                                <input type="date" id="selectedStartDate"
                                                                    name="msche_st_dt" readonly
                                                                    style="margin-top: 10px; margin-bottom: 10px;"> ~
                                                                <input type="date" id="selectedEndDate"
                                                                    name="msche_ed_dt" readonly
                                                                    style="margin-top: 10px; margin-bottom: 10px;">
                                                                <br>
                                                                일정 색상 선택 <br>
                                                                <div class="colorbox">
                                                                    <input type="radio" id="color1" name="msche_color"
                                                                        class="color-radio" value="#df5454">
                                                                    <label for="color1" class="color-label"
                                                                        style="background-color: #df5454;"></label>

                                                                    <input type="radio" id="color2" name="msche_color"
                                                                        class="color-radio" value="#eca957">
                                                                    <label for="color2" class="color-label"
                                                                        style="background-color: #eca957;"></label>

                                                                    <input type="radio" id="color3" name="msche_color"
                                                                        class="color-radio" value="#dfe458">
                                                                    <label for="color3" class="color-label"
                                                                        style="background-color: #dfe458;"></label>

                                                                    <input type="radio" id="color4" name="msche_color"
                                                                        class="color-radio" value="#67ee4c">
                                                                    <label for="color4" class="color-label"
                                                                        style="background-color: #67ee4c;"></label>

                                                                    <input type="radio" id="color5" name="msche_color"
                                                                        class="color-radio" value="#56dae6">
                                                                    <label for="color5" class="color-label"
                                                                        style="background-color: #56dae6;"></label>

                                                                    <input type="radio" id="color6" name="msche_color"
                                                                        class="color-radio" value="#518ff3">
                                                                    <label for="color6" class="color-label"
                                                                        style="background-color: #518ff3;"></label>

                                                                    <input type="radio" id="color7" name="msche_color"
                                                                        class="color-radio" value="#7661ee">
                                                                    <label for="color7" class="color-label"
                                                                        style="background-color: #7661ee;"></label>

                                                                    <input type="radio" id="color8" name="msche_color"
                                                                        class="color-radio" value="#ec62ec">
                                                                    <label for="color8" class="color-label"
                                                                        style="background-color: #ec62ec;"></label>
                                                                </div>
                                                                <input type="hidden" class="mem_id" name="mem_id"
                                                                    value="<%= user.mem_id %>">
                                                            </div>
                                                        </div>
                                                        <div class="modal-footer">
                                                            <button type="button" class="btn btn-secondary"
                                                                data-bs-dismiss="modal">취소</button>
                                                            <button type="submit" class="btn btn-primary"
                                                                id="saveEvent">저장</button>
                                                        </div>
                                                    </form>
                                                </div>
                                            </div>
                                        </div>

                                        <!-- Modal delete-->
                                        <div class="modal fade" id="deleteModal" tabindex="-1"
                                            aria-labelledby="deleteModalLabel" aria-hidden="true">
                                            <form id="deleteForm">
                                                <div class="modal-dialog">
                                                    <div class="modal-content">
                                                        <div class="modal-header">
                                                            <h5 class="modal-title" id="deleteModalLabel">일정 체크</h5>
                                                            <button type="button" class="btn-close"
                                                                data-bs-dismiss="modal" aria-label="Close"></button>
                                                            <input type="hidden" class="mem_id" name="mem_id"
                                                                value="<%= user.mem_id %>">
                                                            <input type="hidden" id="deletedTitle" name="msche_title">
                                                            <input type="hidden" id="deletedStartDate"
                                                                name="msche_st_dt">
                                                            <input type="hidden" id="deletedEndDate" name="msche_ed_dt">
                                                        </div>
                                                        <div class="modal-body">
                                                            <p>일정을 완료하셨나요? </p>
                                                        </div>
                                                        <div class="modal-footer">
                                                            <button type="submit" class="btn btn-primary"
                                                                id="completeEvent">완료</button>
                                                            <button type="submit" class="btn btn-danger"
                                                                id="confirmDelete">삭제</button>
                                                            <button type="button" class="btn btn-secondary"
                                                                data-bs-dismiss="modal">취소</button>
                                                        </div>
                                                    </div>
                                            </form>
                                        </div>
                                    </div>
                            </div>
                            <% }else{ %>
                                <div class="swiper"> <!-- Arvo, Open Sans -->
                                    <h1 class="swiper_h1">HOW TO</h1>
                                    <div class="swiper-wrapper">
                                        <!-- Slides -->
                                        <div class="swiper-slide">
                                            <div class="slide-description">캘린더를 클릭하여 일정 추가</div>
                                            <img src="../img/일정추가.gif" alt="Image 1">
                                        </div>
                                        <div class="swiper-slide">
                                            <div class="slide-description">검색창에 검색하여 일정 검색</div>
                                            <img src="../img/일정-검색.gif" alt="Image 2">
                                        </div>
                                        <div class="swiper-slide">
                                            <div class="slide-description">일정완료율에 따른 일정 수행률</div>
                                            <img src="../img/일정-수행률.gif" alt="Image 3">
                                        </div>
                                        <div class="swiper-slide">
                                            <div class="slide-description">자격증 입력 및 취득한 자격증</div>
                                            <img src="../img/자격증-입력_보여주기.gif" alt="Image 4">
                                        </div>
                                    </div>
                                    <!-- If we need pagination -->
                                    <div class="swiper-pagination"></div>

                                    <!-- If we need navigation buttons -->
                                    <div class="swiper-button-prev"></div>
                                    <div class="swiper-button-next"></div>

                                </div>


                                <% } %>
                                    <!-- Right Column for EJS Template -->
                                    <div class="content-column">
                                        <%- body %>
                                    </div>
                        </div>
                </div>
                <!-- /.container-fluid -->
            </div>
            <!-- End of Main Content -->
            <!-- Footer -->
            <footer class="sticky-footer bg-white">
                <div class="container my-auto">
                    <div class="copyright text-center my-auto">
                        <span>스마트 인재 개발원 SW캠프 언어지능 과정 JOB-A &copy; 문승명, 임형선, 김지성, 이선우</span>
                    </div>
                </div>
            </footer>
            <!-- End of Footer -->
    </div>
    <!-- End of Content Wrapper -->
    </div>
    <!-- End of Page Wrapper -->
    <% if (user){ %>
        <script>
            // 캘린더 스크립트
            document.addEventListener('DOMContentLoaded', function () {
                var calendarEl = document.getElementById('calendar');
                var selectedStartDate, selectedEndDate, eventId;
                const holidaysColor = '#FC9E99';
                var holidays = [
                    { title: '신정', start: '2024-01-01' },
                    { title: '설날', start: '2024-02-10', end: '2024-02-12' },
                    { title: '삼일절', start: '2024-03-01' },
                    { title: '어린이날', start: '2024-05-05' },
                    { title: '부처님 오신 날', start: '2024-05-15' },
                    { title: '현충일', start: '2024-06-06' },
                    { title: '광복절', start: '2024-08-15' },
                    { title: '추석', start: '2024-09-16', end: '2024-09-18' },
                    { title: '개천절', start: '2024-10-03' },
                    { title: '한글날', start: '2024-10-09' },
                    { title: '크리스마스', start: '2024-12-25' },
                    { title: '대체공휴일 (설날)', start: '2024-02-12' },
                    { title: '대체공휴일 (어린이날)', start: '2024-05-06' },
                    { title: '대체공휴일 (추석)', start: '2024-09-18' }
                ].map(event => ({ ...event, color: holidaysColor }));;


                var calendar = new FullCalendar.Calendar(calendarEl, {
                    locale: 'ko',
                    initialView: 'dayGridMonth',
                    selectable: true,
                    // 드래그로 일정추가
                    select: function (info) {
                        selectedStartDate = info.startStr
                        selectedEndDate = info.endStr
                        var eventModal = new bootstrap.Modal(document.getElementById('eventModal'));
                        document.getElementById('selectedStartDate').value = selectedStartDate;
                        document.getElementById('selectedEndDate').value = selectedEndDate;
                        eventModal.show();
                    },
                    // 클릭으로 일정 삭제
                    eventClick: function (info) {
                        selectedEvent = info.event;
                        document.getElementById('deletedTitle').value = selectedEvent.title;
                        document.getElementById('deletedStartDate').value = selectedEvent.startStr;
                        document.getElementById('deletedEndDate').value = selectedEvent.endStr;
                        var deleteModal = new bootstrap.Modal(document.getElementById('deleteModal'));
                        deleteModal.show();
                    },
                    dayCellContent: function (info) {
                        var number = document.createElement("a");
                        number.classList.add("fc-daygrid-day-number")
                        number.innerHTML = info.dayNumberText.replace("일", '').replace("日", "");
                        if (info.view.type === "dayGridMonth") {
                            return {
                                html: number.outerHTML
                            };
                        }
                        return {
                            domNodes: []
                        };
                    },
                    eventSources: [{
                        events: function (fetchInfo, successCallback, failureCallback) {
                            $.ajax({
                                url: '/schedule/mschedule',
                                type: 'GET',
                                dataType: 'json',
                                success: function (data) {
                                    successCallback(data);
                                },
                                error: function () {
                                    failureCallback();
                                    console.log('실패');
                                }
                            });
                        }
                    },
                    {
                        events: holidays
                    }
                    ]

                });


                calendar.render();

                var saveButton = document.getElementById('saveEvent');

                // 입력 필드의 값을 모니터링하여 변경이 있을 때마다 확인합니다.
                document.getElementById('eventTitle').addEventListener('input', function () {
                    // 입력 필드의 값이 비어 있는지 확인합니다.
                    if (this.value.trim() === '') {
                        // 값이 비어 있으면 저장 버튼을 비활성화합니다.
                        saveButton.disabled = true;
                    } else {
                        // 값이 채워져 있으면 저장 버튼을 활성화합니다.
                        saveButton.disabled = false;
                    }
                });

                // 초기에도 입력값이 없으면 저장 버튼을 비활성화합니다.
                if (document.getElementById('eventTitle').value.trim() === '') {
                    saveButton.disabled = true;
                }

                document.getElementById('saveEvent').addEventListener('click', function () {
                    var eventTitle = document.getElementById('eventTitle').value;
                    var selectedColor = document.querySelector('.color-radio:checked').value;
                    var addString = $("#eventForm").serialize();
                    $.ajax({
                        type: 'post',
                        url: '/schedule/add',
                        data: addString,
                        error: function (xhr, status, error) {
                            console.log("실패");
                        },
                        success: function (json) {
                            console.log("성공");
                            window.location.reload('/today');
                        }
                    });
                    if (eventTitle) {
                        calendar.addEvent({
                            title: eventTitle,
                            start: selectedStartDate,
                            end: selectedEndDate,
                            color: selectedColor
                        });
                        var eventModal = bootstrap.Modal.getInstance(document.getElementById('eventModal'));
                        eventModal.hide();
                        document.getElementById('eventForm').reset();
                    }
                });
                document.getElementById('confirmDelete').addEventListener('click', function (event) {
                    event.preventDefault();
                    if (selectedEvent) {
                        selectedEvent.remove();
                        var deleteString = $("#deleteForm").serialize();
                        $.ajax({
                            type: 'post',
                            url: '/schedule/remove',
                            data: deleteString,
                            error: function (xhr, status, error) {
                                console.log("실패");
                            },
                            success: function (json) {
                                console.log("성공");
                            }
                        });
                        var deleteModal = bootstrap.Modal.getInstance(document.getElementById('deleteModal'));
                        deleteModal.hide();
                    }
                });
                document.getElementById('completeEvent').addEventListener('click', function (event) {
                    event.preventDefault();
                    if (selectedEvent) {
                        var deleteString = $("#deleteForm").serialize();
                        $.ajax({
                            type: 'post',
                            url: '/schedule/complete',
                            data: deleteString,
                            error: function (xhr, status, error) {
                                console.log("실패");
                            },
                            success: function (json) {
                                console.log("성공");
                            }
                        });
                        var deleteModal = bootstrap.Modal.getInstance(document.getElementById('deleteModal'));
                        deleteModal.hide();
                    }
                });
                window.addSchedule = function (cert) {
                    const scheduleData = {
                        자격명: cert.자격명,
                        회차: cert.회차,
                        차수: cert.차수,
                        접수시작일자: cert.접수시작일자,
                        접수종료일자: cert.접수종료일자,
                        시험시작일자: cert.시험시작일자,
                        시험종료일자: cert.시험종료일자,
                        합격자발표: cert.합격자발표,
                        일정컬러: '#7661ee',
                        mem_id: '<%= user.mem_id %>'
                    };

                    const cha = cert.차수 === null ? " ": cert.차수;
                    const schedules = [
                        {
                            title: `${cert.회차} ${cert.자격명} ${cha} 접수`,
                            start: cert.접수시작일자,
                            end: cert.접수종료일자,
                            color: '#7661ee'
                        },
                        {
                            title: `${cert.회차} ${cert.자격명} ${cha} 시험`,
                            start: cert.시험시작일자,
                            end: cert.시험종료일자,
                            color: '#7661ee'
                        },
                        {
                            title: `${cert.자격명} 합격자발표`,
                            start: cert.합격자발표,
                            end: cert.합격자발표,
                            color: '#7661ee'
                        }
                    ];

                    schedules.forEach(schedule => {

                        $.ajax({
                            type: 'post',
                            url: '/schedule/add',
                            data: {
                                mem_id: scheduleData.mem_id,
                                msche_title: schedule.title,
                                msche_st_dt: schedule.start,
                                msche_ed_dt: schedule.end,
                                msche_color: schedule.color
                            },
                            error: function (xhr, status, error) {
                                console.log("일정 추가 실패");
                            },
                            success: function (json) {
                                window.location.reload('/');
                                console.log("일정 추가 성공");
                            }
                        });
                    });

                    alert('자격증 일정이 성공적으로 추가되었습니다!');
                };
            });

        </script>
        <% } %>
            <script>
                document.addEventListener('DOMContentLoaded', function () {
                    const swiper = new Swiper('.swiper', {
                        // Optional parameters
                        direction: 'horizontal',
                        loop: true,

                        // If we need pagination
                        pagination: {
                            el: '.swiper-pagination',
                        },

                        // Navigation arrows
                        navigation: {
                            nextEl: '.swiper-button-next',
                            prevEl: '.swiper-button-prev',
                        },

                        // And if we need scrollbar
                        scrollbar: {
                            el: '.swiper-scrollbar',
                        },
                    });
                });
            </script>
            <!-- 기존 스크립트 태그들 -->
</body>
<!-- Bootstrap core JavaScript-->
<script src="vendor/jquery/jquery.min.js"></script>
<script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
<!-- Core plugin JavaScript-->
<script src="vendor/jquery-easing/jquery.easing.min.js"></script>
<!-- Custom scripts for all pages-->
<script src="js/sb-admin-2.min.js"></script>
<!-- Page level plugins -->
<script src="vendor/chart.js/Chart.min.js"></script>
<!-- Page level custom scripts -->
<script src="js/demo/chart-area-demo.js"></script>
<script src="js/demo/chart-pie-demo.js"></script>
<script src="js/demo/chart-bar-demo.js"></script>
<script>
    // 로그인을 안할시엔 띄워주기
    $(document).on('click', '#login_plz', function (e) {
        Swal.fire({
            text: '로그인이 필요한 기능입니다',
            confirmButtonColor: '#ffcd00',
            confirmButtonText: '확인'
        });
    });

</script>
<!-- FullCalender template -->
<link href='https://cdn.jsdelivr.net/npm/fullcalendar@5.11.0/main.min.css' rel='stylesheet' />
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<script src='https://cdn.jsdelivr.net/npm/fullcalendar@5.11.0/main.min.js'></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</html>