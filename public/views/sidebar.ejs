    <!-- Sidebar -->
    <link rel="stylesheet" href="../css/slider.css">
    <ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">
        <!-- Sidebar - Brand -->
        <a class="sidebar-brand d-flex align-items-center justify-content-center" href="/">
            <div class="sidebar-brand-icon rotate-n-15">
                <img src="../img/메인-removebg-preview__1_-removebg-preview.png" alt="설명" width="60" height="60">
            </div>
            <div class="sidebar-brand-text mx-3">J O B <sup>a</sup></div>
        </a>
        
        <!-- Divider -->
        <hr class="sidebar-divider my-0">
        <hr class="sidebar-divider my-0">

        <!-- Nav Item - Dashboard -->
        <li class="nav-item active">
            <a class="studyg"  href="/">
                <i class="fas fa-fw fa-calendar sideBlockIcon"></i>
                <span class="sideBlockText">일정 관리</span></a>
        </li>

        <!-- Divider -->
        <hr class="sidebar-divider my-0">
        <hr class="sidebar-divider my-0">

        <!-- Nav Item - 자격증 -->
        <li class="nav-item">
            <a class="studyg " <% if (user) { %> href="/certificate" <% } else { %> id="login_plz" <% } %> data-target="#collapseTwo"
                aria-expanded="true" aria-controls="collapseTwo">
                <i class="fas fa-fw fa-cog sideBlockIcon"></i>
                <span class="sideBlockText">자격증 AI</span></a>
        </li>

        <!-- Divider -->
        <hr class="sidebar-divider my-0">
        <hr class="sidebar-divider my-0">

        <!-- Nav Item - 스터디그룹 -->
        <li class="nav-item">
            <a class="studyg" <% if (user) { %> href="/studyGruop" <% } else { %> id="login_plz" <% } %>>
                <i id="dddd" class="fas fa-fw fa-cog sideBlockIcon"></i>
                <span id="dddd"  class="sideBlockText">스터디그룹</span></a>
        </li>

        <!-- Divider -->
        <hr class="sidebar-divider my-0">
        <hr class="sidebar-divider my-0">

        <!-- Sidebar Toggler (Sidebar) -->
        <div class="text-center d-none d-md-inline">
            <button class="rounded-circle border-0" id="sidebarToggle"></button>
        </div>
    </ul>
    <!-- End of Sidebar -->
<% if(user) {%>
<script>

document.addEventListener('DOMContentLoaded', function() {
  // 클래스 이름이 "studyg"인 모든 <a> 태그를 선택
  const studygLinks = document.querySelectorAll('a.studyg');
  const navbarNav = document.querySelector('.d-flex');

  // 로컬 스토리지에서 색상 및 굵기 정보를 가져와 적용
  studygLinks.forEach(function(link, index) {
    const color = localStorage.getItem('studygLinkColor' + index);
    const fontWeight = localStorage.getItem('studygLinkFontWeight' + index);
    if (color) {
      link.style.color = color;
    }
    if (fontWeight) {
      link.style.fontWeight = fontWeight;
    }
  });
  navbarNav.addEventListener('click', function(event) {
    if (event.currentTarget === navbarNav) {
      // navbar-nav 클래스를 가진 요소 자체를 클릭한 경우

      // 첫 번째 링크(index 0)를 하얀색과 굵게 처리
      studygLinks[0].style.color = 'white';
      studygLinks[0].style.fontWeight = 'bold';
      localStorage.setItem('studygLinkColor0', 'white');
      localStorage.setItem('studygLinkFontWeight0', 'bold');

      // 다른 링크 초기화
      for (let i = 1; i < studygLinks.length; i++) {
        studygLinks[i].style.color = 'rgba(201, 201, 201, 0.8)';
        studygLinks[i].style.fontWeight = 'normal';
        localStorage.setItem('studygLinkColor' + i, 'rgba(201, 201, 201, 0.8)');
        localStorage.setItem('studygLinkFontWeight' + i, 'normal');
      }
    }
  });

  // 선택된 모든 요소에 대해 반복문을 통해 클릭 이벤트 리스너 추가
  studygLinks.forEach(function(link, index) {
    link.addEventListener('click', function(event) {
      // 기본 동작 막기 (링크의 href로 이동하지 않도록)
      // 모든 링크의 색상과 글씨 굵기를 초기화 (회색, 기본 굵기)
      studygLinks.forEach(function(otherLink, otherIndex) {
        otherLink.style.color = 'rgba(201, 201, 201, 0.8)';
        otherLink.style.fontWeight = 'normal';
        localStorage.setItem('studygLinkColor' + otherIndex, 'rgba(201, 201, 201, 0.8)');
        localStorage.setItem('studygLinkFontWeight' + otherIndex, 'normal');
      });

      // 클릭된 링크의 색상을 하얀색으로 변경하고 글씨를 굵게 함
      link.style.color = 'white';
      link.style.fontWeight = 'bold';
      localStorage.setItem('studygLinkColor' + index, 'white');
      localStorage.setItem('studygLinkFontWeight' + index, 'bold');

      // 링크의 기본 동작을 수행 (이동)
    
    });
  });
});
</script>
<% } %>